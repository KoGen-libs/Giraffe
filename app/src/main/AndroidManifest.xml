<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GiraffeApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Graffe"
        android:usesCleartextTraffic="true">
        <!-- This app only ever talks to a local dev server (TestGrpc) over plain http:// on the
             same LAN - it's never published, so an app-wide allowance is fine here rather than a
             network security config scoped to specific domains. gRPC's plaintext channel wasn't
             affected by the OS's cleartext block (different transport stack); OkHttp/Retrofit's
             REST client was. -->


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>